% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/saveQCEpageCardFiles.r
\name{saveQCEcardFiles}
\alias{saveQCEcardFiles}
\title{Write a card placement list and its sidecars}
\usage{
saveQCEcardFiles(QCEcardPlacement, cardsFile, sidecars, dir = ".")
}
\arguments{
\item{QCEcardPlacement}{A placement list from
\code{\link{addCardToQCEcardPlacement}}.}

\item{cardsFile}{Path to write the placement list to, e.g. "cards1.json". Name
it whatever you referenced from the group's \code{cards} argument.}

\item{sidecars}{A NAMED list of \code{\link{buildQCEcardSidecar}} results, the
names being card base names.}

\item{dir}{Directory to write into. DEFAULT = "." (the working directory).}
}
\value{
Invisibly, a character vector of the paths written.
}
\description{
Writes the placement list to \code{cardsFile} and each sidecar to
\code{<name>.card.json} in the same directory. An optional \code{<name>.html}
shell is yours to author if you want one; a card renders from its template
without it.
}
\details{
Unlike a page, a card's sidecar is not optional -- it carries the template and
fields that are the card's entire content.
}
\examples{
cards <- addCardToQCEcardPlacement(NULL, "progress")
saveQCEcardFiles(cards, "cards1.json",
                 sidecars = list(progress = buildQCEcardSidecar(
                   template = "<div>{deadlineRemaining}</div>", refreshMs = 1000)),
                 dir = tempdir())
}
\keyword{QCE}
\keyword{card}
\keyword{cards}
\keyword{save}
\keyword{write}
