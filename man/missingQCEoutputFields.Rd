% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buildQCEoutputFieldManifest.r
\name{missingQCEoutputFields}
\alias{missingQCEoutputFields}
\title{Which expected columns are missing from a fields.txt}
\usage{
missingQCEoutputFields(dir, fieldsFile = file.path(dir, "fields.txt"))
}
\arguments{
\item{dir}{The experiment directory holding the JSON config files.}

\item{fieldsFile}{Path to the \code{fields.txt} to check. DEFAULT =
\code{file.path(dir, "fields.txt")}.}
}
\value{
A character vector of expected columns absent from the file; empty
  when nothing is missing.
}
\description{
The machine-readable half of \code{\link{buildQCEoutputFieldManifest}}: same
scan, but it returns the column names instead of a report to read. Use it to
make a build script FAIL when the config produces a column the whitelist does
not carry, rather than leaving the gap to be noticed in the data later.
}
\examples{
# In a build script, right after writing fields.txt:
# gaps <- missingQCEoutputFields("myExperiment/")
# if (length(gaps)) stop("fields.txt is missing: ", paste(gaps, collapse = ", "))
}
\keyword{QCE}
\keyword{columns}
\keyword{fields}
\keyword{fields.txt}
\keyword{manifest}
\keyword{output}
